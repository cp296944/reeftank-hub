[Unit]
Description=ReefTank Hub install OpenThread Border Router
After=network-online.target docker.service reeftank-thread-flash.service

[Service]
Type=oneshot
User=root
ExecStart=/opt/reeftank-hub/thread/bin/install.sh --confirm
TimeoutStartSec=30min
